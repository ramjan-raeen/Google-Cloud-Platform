1. Logical Query Processing Order (Conceptual)

SQL always has a logical order, independent of how the engine optimizes it:

* FROM

* ON

* JOIN

* WHERE

* GROUP BY

* HAVING

* SELECT

* ORDER BY

* LIMIT